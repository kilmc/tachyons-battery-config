const backgroundSize = {
  prop: "background-size",
  propName: "bg",
  keywordValues: {
    separator: "-",
    values: {
      cover: "cover",
      contain: "contain"
    }
  }
};

export default backgroundSize;
