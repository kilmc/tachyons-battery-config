const position = {
  prop: "position",
  propName: "",
  keywordValues: {
    values: {
      static: "static",
      relative: "relative",
      absolute: "absolute",
      fixed: "fixed"
    }
  }
};

export default position;
